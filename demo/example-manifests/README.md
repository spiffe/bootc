# Example manifests

Here are some examples you can add to your manifests.

In each example, we assume the hostname is `foo`. Update as needed.

* `node/` - contains examples for each node and spire-ha-agent entries
* `sshd/` - contains an example of setting up an sshd entry
