apiVersion: spire.spiffe.io/v1alpha1
kind: ClusterStaticEntry
metadata:
  name: spire-server-b-spiffe-step-ssh-server-fetchca
spec:
  parentID: spiffe://${SPIFFE_TRUST_DOMAIN}/node/spire-server-b.${SPIFFE_TRUST_DOMAIN}
  spiffeID: spiffe://${SPIFFE_TRUST_DOMAIN}/spiffe-step-ssh-fetchca
  dnsNames:
  - "spiffe-step-ssh-fetchca-b.${SPIFFE_TRUST_DOMAIN}"
  selectors:
  - systemd:id:spiffe-step-ssh-fetchca@b.service
